crie o javascript para perfil-usuario:

html:

<!doctype html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="./assets/css/paginas/perfil-usuario.css">
    <title>Meu Perfil</title>
  </head>

  <body>
    <header class="header">
      <a href="index.html" class="logo">PetShow</a>

      <nav class="nav">
        <a href="index.html">Página Inicial</a>
        <a href="perfil-usuario.html">Meu Perfil</a>
        <a href="login.html">Sair</a>
      </nav>
    </header>

    <main class="container">
      <section class="card-perfil">
        <h1>Meu Perfil</h1>

        <form>
          <div class="campo">
            <label for="nome">Nome</label>
            <input
              type="text"
              id="nome"
              placeholder="Digite seu nome"
              maxlength="100"
              required
            />
          </div>

          <div class="campo">
            <label for="cpf">CPF</label>
            <input
              type="text"
              id="cpf"
              placeholder="CPF"
              disabled
            />
          </div>

          <div class="campo">
            <label for="email">E-mail</label>
            <input
              type="email"
              id="email"
              placeholder="Digite seu e-mail"
              maxlength="100"
              required
            />
          </div>

          <div class="campo">
            <label for="telefone">Telefone</label>
            <input
              type="tel"
              id="telefone"
              placeholder="Digite seu telefone"
              maxlength="20"
            />
          </div>

          <div class="campo">
            <label for="senha">Nova senha</label>
            <input
              type="password"
              id="senha"
              placeholder="Digite uma nova senha"
            />
          </div>

          <div class="campo">
            <label for="confirmeSenha">Confirmar nova senha</label>
            <input
              type="password"
              id="confirmeSenha"
              placeholder="Confirme a nova senha"
            />
          </div>

          <button class="btn-confirm" type="submit">
            Atualizar dados
          </button>

          <p class="link">
            <a href="index.html">Voltar para a página inicial</a>
          </p>
        </form>
      </section>
    </main>

    <footer class="footer">
      <p>© 2026 PetShow - Sistema de Agendamento</p>
    </footer>

    <script type="module" src="./assets/js/paginas/perfil-usuario.js"></script>
  </body>
</html>

css:

@import url("../global/reset.css");
@import url("../componentes/botoes.css");
@import url("../componentes/cabecalho.css");
@import url("../componentes/footer.css");

body {
  font-family: Arial, Helvetica, sans-serif;
  background-color: #f5f5f5;
}

.container {
  width: 100%;
  max-width: 400px;
  margin: 50px auto;
  background-color: white;
  padding: 20px;
  border: 1px solid #ccc;
  border-radius: 8px;
}

h1 {
  text-align: center;
  margin-bottom: 20px;
  font-size: 24px;
}

form {
  display: flex;
  flex-direction: column;
  gap: 15px;
}

.campo {
  display: flex;
  flex-direction: column;
}

label {
  margin-bottom: 5px;
}

input {
  padding: 10px;
  border: 1px solid #ccc;
}

input:disabled {
  background-color: #f0f0f0;
  color: #666;
  cursor: not-allowed;
}

.link {
  text-align: center;
}

.link a {
  color: #333;
  text-decoration: none;
}

.link a:hover {
  text-decoration: underline;
}

use o seguinte js como base (cadastro.js):

const form = document.querySelector('form')
const cpfInput = document.querySelector('#cpf')
const telefone = document.querySelector('#telefone')

form.addEventListener('submit',async function(event) {
    event.preventDefault();

    const nome = document.querySelector('#nome').value
    const cpf = document.querySelector('#cpf').value
    const email = document.querySelector('#email').value
    const senha = document.querySelector('#senha').value
    const confirmeSenha = document.querySelector('#confirmeSenha').value
    const telefone = document.querySelector('#telefone').value

    if (senha != confirmeSenha) {
      alert("As senhas não são iguais")
      return;
    }

    const usuario = {
      nome: nome,
      cpf: cpf,
      email: email,
      senha: senha,
      telefone: telefone
    }

    try {
      const resposta = await fetch('http://localhost:3000/api/usuarios', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json'
        },
        body: JSON.stringify(usuario)
      })

      if (!resposta.ok) {
        throw  new Error('Erro ao cadastrar usuário!')
      }

      alert('Usuário cadastrado com sucesso!')

      window.location.href = 'login.html'
    } catch (erro) {
      alert('Erro ao cadastrar usuário')
    }

})

js do login:


const form = document.querySelector('form');

form.addEventListener('submit', async function (event) {
  event.preventDefault();

  const email = document.querySelector('#email').value;
  const senha = document.querySelector('#senha').value;

  const usuario = {
    email: email,
    senha: senha,
  };

  try {
    const resposta = await fetch('http://localhost:3000/api/login', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify(usuario),
    });

    const dados = await resposta.json();

    if (!resposta.ok) {
      alert(dados.erro);
      return;
    }

    localStorage.setItem('usuario', JSON.stringify(dados.usuario))

    alert('Login realizado!');
  } catch (erro) {
    alert('Erro ao conectar com o servidor');
  }
});


